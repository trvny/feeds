name: Kanarek favorites widget bootstrap

on:
  push:
    branches: [feat/kanarek-favorites-widget-controls]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/kanarek-favorites-widget-controls
          fetch-depth: 0

      - name: Apply feature patch
        run: |
          python <<'PY'
          from pathlib import Path
          import xml.etree.ElementTree as ET

          def replace(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one match, found {count}: {old[:80]!r}")
              p.write_text(text.replace(old, new))

          settings = "kanarek/app/src/main/java/com/kanarek/data/SettingsStore.kt"
          replace(
              settings,
              '''    /** User-curated set of cover-worthy source names (case-insensitive). */
    val topSources: Flow<Set<String>> =
        context.dataStore.data.map { prefs ->
            decodeSources(prefs[KEY_TOP_SOURCES])
        }

    /** Radio/IPTV station list (the player screen), stored as M3U text — same format as import/export. */''',
              '''    /** User-curated set of cover-worthy source names (case-insensitive). */
    val topSources: Flow<Set<String>> =
        context.dataStore.data.map { prefs ->
            decodeSources(prefs[KEY_TOP_SOURCES])
        }

    /** Stable station ids starred by the user; kept separate from portable M3U data. */
    val favoriteStationIds: Flow<Set<String>> =
        context.dataStore.data.map { prefs ->
            decodeSources(prefs[KEY_FAVORITE_STATIONS])
        }

    /** Radio/IPTV station list (the player screen), stored as M3U text — same format as import/export. */''',
          )
          replace(
              settings,
              '''    suspend fun setTopSources(sources: Set<String>) {
        context.dataStore.edit { it[KEY_TOP_SOURCES] = encodeSources(sources) }
    }

    suspend fun setStations(stations: List<Station>) {''',
              '''    suspend fun setTopSources(sources: Set<String>) {
        context.dataStore.edit { it[KEY_TOP_SOURCES] = encodeSources(sources) }
    }

    suspend fun setFavoriteStationIds(ids: Set<String>) {
        context.dataStore.edit { it[KEY_FAVORITE_STATIONS] = encodeSources(ids) }
    }

    suspend fun setStations(stations: List<Station>) {''',
          )
          replace(
              settings,
              '''        private val KEY_TOP_SOURCES = stringPreferencesKey("top_sources")
        private val KEY_STATIONS = stringPreferencesKey("stations")''',
              '''        private val KEY_TOP_SOURCES = stringPreferencesKey("top_sources")
        private val KEY_FAVORITE_STATIONS = stringPreferencesKey("favorite_station_ids")
        private val KEY_STATIONS = stringPreferencesKey("stations")''',
          )

          player = "kanarek/app/src/main/java/com/kanarek/ui/PlayerScreen.kt"
          replace(
              player,
              '''import androidx.compose.material.icons.filled.SkipNext
import androidx.compose.material.icons.filled.SkipPrevious
import androidx.compose.material.icons.filled.Tv''',
              '''import androidx.compose.material.icons.filled.SkipNext
import androidx.compose.material.icons.filled.SkipPrevious
import androidx.compose.material.icons.filled.Star
import androidx.compose.material.icons.filled.StarBorder
import androidx.compose.material.icons.filled.Tv''',
          )
          replace(
              player,
              '''    val stations by settings.stations.collectAsStateWithLifecycle(initialValue = emptyList())
    val backendUrl by settings.backendUrl.collectAsStateWithLifecycle(initialValue = "")''',
              '''    val stations by settings.stations.collectAsStateWithLifecycle(initialValue = emptyList())
    val favoriteStationIds by settings.favoriteStationIds.collectAsStateWithLifecycle(initialValue = emptySet())
    val backendUrl by settings.backendUrl.collectAsStateWithLifecycle(initialValue = "")''',
          )
          replace(
              player,
              '''    val hasTv = remember(stations) { stations.any { it.kind == StationKind.TV } }
    val hasRadio = remember(stations) { stations.any { it.kind == StationKind.RADIO } }
    val hasOther = remember(stations) { stations.any { it.kind == StationKind.UNKNOWN } }
    val tabs =
        remember(hasRadio, hasTv, hasOther) {
            buildList {
                if (hasRadio) add(StationFilter.RADIO)
                if (hasTv) add(StationFilter.TV)
                if (hasOther) add(StationFilter.OTHER)
            }
        }
    val showTabs = tabs.size > 1
    // Keep the selected tab valid as stations come and go (e.g. the last radio station gets
    // deleted while sitting on the Radio tab).
    LaunchedEffect(tabs) {
        if (tabs.isNotEmpty() && kindFilter !in tabs) kindFilter = tabs.first()
    }''',
              '''    val hasTv = remember(stations) { stations.any { it.kind == StationKind.TV } }
    val hasRadio = remember(stations) { stations.any { it.kind == StationKind.RADIO } }
    val hasOther = remember(stations) { stations.any { it.kind == StationKind.UNKNOWN } }
    val hasFavorites = remember(stations, favoriteStationIds) { stations.any { it.id in favoriteStationIds } }
    val tabs =
        remember(hasFavorites, hasRadio, hasTv, hasOther) {
            buildList {
                if (hasFavorites) add(StationFilter.FAVORITES)
                if (hasRadio) add(StationFilter.RADIO)
                if (hasTv) add(StationFilter.TV)
                if (hasOther) add(StationFilter.OTHER)
            }
        }
    val showTabs = tabs.size > 1
    // Keep the selected tab valid as stations or favorites come and go. Prefer a media-kind tab
    // when the current filter disappears, so removing the last favorite does not strand the UI.
    LaunchedEffect(tabs) {
        if (tabs.isNotEmpty() && kindFilter !in tabs) {
            kindFilter = tabs.firstOrNull { it != StationFilter.FAVORITES } ?: tabs.first()
        }
    }''',
          )
          replace(
              player,
              '''    fun play(station: Station) {
        bound?.playStationById(station.id)
    }

    var editing by remember { mutableStateOf<Station?>(null) }''',
              '''    fun play(station: Station) {
        bound?.playStationById(station.id)
    }

    fun toggleFavorite(station: Station) {
        val next = favoriteStationIds.toMutableSet()
        if (!next.add(station.id)) next.remove(station.id)
        scope.launch { settings.setFavoriteStationIds(next) }
    }

    fun deleteStation(station: Station) {
        persist(stations.filterNot { it.id == station.id })
        if (station.id in favoriteStationIds) {
            scope.launch { settings.setFavoriteStationIds(favoriteStationIds - station.id) }
        }
    }

    var editing by remember { mutableStateOf<Station?>(null) }''',
          )
          replace(
              player,
              '''    LaunchedEffect(currentStation?.id) {
        val target =
            when (currentStation?.kind) {
                StationKind.TV -> StationFilter.TV
                StationKind.RADIO -> StationFilter.RADIO
                StationKind.UNKNOWN -> StationFilter.OTHER
                null -> null
            }
        if (target != null && target in tabs) kindFilter = target
    }''',
              '''    LaunchedEffect(currentStation?.id, favoriteStationIds) {
        val current = currentStation ?: return@LaunchedEffect
        if (kindFilter == StationFilter.FAVORITES && current.id in favoriteStationIds) return@LaunchedEffect
        val target =
            when (current.kind) {
                StationKind.TV -> StationFilter.TV
                StationKind.RADIO -> StationFilter.RADIO
                StationKind.UNKNOWN -> StationFilter.OTHER
            }
        if (target in tabs) kindFilter = target
    }''',
          )
          replace(
              player,
              '''                        IconButton(onClick = { bound?.previous() }) {
                            Icon(Icons.Filled.SkipPrevious, contentDescription = stringResource(R.string.action_previous))
                        }''',
              '''                        IconButton(onClick = { toggleFavorite(currentStation) }) {
                            Icon(
                                if (currentStation.id in favoriteStationIds) Icons.Filled.Star else Icons.Filled.StarBorder,
                                contentDescription =
                                    stringResource(
                                        if (currentStation.id in favoriteStationIds) {
                                            R.string.remove_station_favorite
                                        } else {
                                            R.string.add_station_favorite
                                        },
                                    ),
                            )
                        }
                        IconButton(onClick = { bound?.previous() }) {
                            Icon(Icons.Filled.SkipPrevious, contentDescription = stringResource(R.string.action_previous))
                        }''',
          )
          replace(
              player,
              '''            val visible =
                remember(stations, kindFilter, showTabs) {
                    if (!showTabs) {
                        stations
                    } else {
                        when (kindFilter) {
                            StationFilter.TV -> stations.filter { it.kind == StationKind.TV }
                            StationFilter.RADIO -> stations.filter { it.kind == StationKind.RADIO }
                            StationFilter.OTHER -> stations.filter { it.kind == StationKind.UNKNOWN }
                        }
                    }
                }''',
              '''            val visible =
                remember(stations, favoriteStationIds, kindFilter, showTabs) {
                    if (!showTabs) {
                        stations
                    } else {
                        when (kindFilter) {
                            StationFilter.FAVORITES -> stations.filter { it.id in favoriteStationIds }
                            StationFilter.TV -> stations.filter { it.kind == StationKind.TV }
                            StationFilter.RADIO -> stations.filter { it.kind == StationKind.RADIO }
                            StationFilter.OTHER -> stations.filter { it.kind == StationKind.UNKNOWN }
                        }
                    }
                }''',
          )
          replace(
              player,
              '''                            StationRow(
                                station = station,
                                isCurrent = station.id == currentStation?.id,
                                onClick = { play(station) },
                                onEdit = { editing = station },
                                onDelete = { persist(stations.filterNot { it.id == station.id }) },
                            )''',
              '''                            StationRow(
                                station = station,
                                isCurrent = station.id == currentStation?.id,
                                isFavorite = station.id in favoriteStationIds,
                                actions =
                                    StationRowActions(
                                        onPlay = { play(station) },
                                        onToggleFavorite = { toggleFavorite(station) },
                                        onEdit = { editing = station },
                                        onDelete = { deleteStation(station) },
                                    ),
                            )''',
          )
          replace(
              player,
              '''                            val isCollapsed = collapsed[key] ?: true''',
              '''                            val isCollapsed = collapsed[key] ?: (kindFilter != StationFilter.FAVORITES)''',
          )
          replace(
              player,
              '''                                    StationRow(
                                        station = station,
                                        isCurrent = station.id == currentStation?.id,
                                        onClick = { play(station) },
                                        onEdit = { editing = station },
                                        onDelete = { persist(stations.filterNot { it.id == station.id }) },
                                        showGroupSubtitle = false,
                                    )''',
              '''                                    StationRow(
                                        station = station,
                                        isCurrent = station.id == currentStation?.id,
                                        isFavorite = station.id in favoriteStationIds,
                                        actions =
                                            StationRowActions(
                                                onPlay = { play(station) },
                                                onToggleFavorite = { toggleFavorite(station) },
                                                onEdit = { editing = station },
                                                onDelete = { deleteStation(station) },
                                            ),
                                        showGroupSubtitle = false,
                                    )''',
          )
          replace(
              player,
              '''            onSave = { station ->
                persist(stations.map { if (it.id == current.id) station else it }.distinctBy { it.id })
                editing = null
            },''',
              '''            onSave = { station ->
                persist(stations.map { if (it.id == current.id) station else it }.distinctBy { it.id })
                if (current.id in favoriteStationIds && station.id != current.id) {
                    scope.launch {
                        settings.setFavoriteStationIds((favoriteStationIds - current.id) + station.id)
                    }
                }
                editing = null
            },''',
          )
          replace(
              player,
              '''private enum class StationFilter { RADIO, TV, OTHER }''',
              '''private enum class StationFilter { FAVORITES, RADIO, TV, OTHER }''',
          )
          replace(
              player,
              '''    when (filter) {
        StationFilter.RADIO -> R.string.filter_radio
        StationFilter.TV -> R.string.filter_tv
        StationFilter.OTHER -> R.string.filter_other
    }''',
              '''    when (filter) {
        StationFilter.FAVORITES -> R.string.filter_favorites
        StationFilter.RADIO -> R.string.filter_radio
        StationFilter.TV -> R.string.filter_tv
        StationFilter.OTHER -> R.string.filter_other
    }''',
          )
          replace(
              player,
              '''    when (filter) {
        StationFilter.RADIO -> Icons.Filled.Radio
        StationFilter.TV -> Icons.Filled.Tv
        StationFilter.OTHER -> null
    }''',
              '''    when (filter) {
        StationFilter.FAVORITES -> Icons.Filled.Star
        StationFilter.RADIO -> Icons.Filled.Radio
        StationFilter.TV -> Icons.Filled.Tv
        StationFilter.OTHER -> null
    }''',
          )
          replace(
              player,
              '''@Composable
private fun StationRow(
    station: Station,
    isCurrent: Boolean,
    onClick: () -> Unit,
    onEdit: () -> Unit,
    onDelete: () -> Unit,
    showGroupSubtitle: Boolean = true,
) {
    Row(
        modifier =
            Modifier
                .fillMaxWidth()
                .clickable(onClick = onClick)''',
              '''private data class StationRowActions(
    val onPlay: () -> Unit,
    val onToggleFavorite: () -> Unit,
    val onEdit: () -> Unit,
    val onDelete: () -> Unit,
)

@Composable
private fun StationRow(
    station: Station,
    isCurrent: Boolean,
    isFavorite: Boolean,
    actions: StationRowActions,
    showGroupSubtitle: Boolean = true,
) {
    Row(
        modifier =
            Modifier
                .fillMaxWidth()
                .clickable(onClick = actions.onPlay)''',
          )
          replace(
              player,
              '''        IconButton(onClick = onEdit) { Icon(Icons.Filled.Edit, contentDescription = stringResource(R.string.edit_station)) }
        IconButton(onClick = onDelete) { Icon(Icons.Filled.Delete, contentDescription = stringResource(R.string.delete_station)) }''',
              '''        IconButton(onClick = actions.onToggleFavorite) {
            Icon(
                if (isFavorite) Icons.Filled.Star else Icons.Filled.StarBorder,
                contentDescription =
                    stringResource(
                        if (isFavorite) R.string.remove_station_favorite else R.string.add_station_favorite,
                    ),
            )
        }
        IconButton(onClick = actions.onEdit) { Icon(Icons.Filled.Edit, contentDescription = stringResource(R.string.edit_station)) }
        IconButton(onClick = actions.onDelete) { Icon(Icons.Filled.Delete, contentDescription = stringResource(R.string.delete_station)) }''',
          )

          provider = "kanarek/app/src/main/java/com/kanarek/widget/KanarekWidgetProvider.kt"
          replace(
              provider,
              '''import com.kanarek.R
''',
              '''import com.kanarek.R
import com.kanarek.data.SettingsStore
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.first
import kotlinx.coroutines.launch
''',
          )
          replace(
              provider,
              '''    ) {
        ids.forEach { id -> renderWidget(context, manager, id) }
        WidgetRefreshWorker.schedule(context)
    }''',
              '''    ) {
        val pendingResult = goAsync()
        CoroutineScope(Dispatchers.IO).launch {
            try {
                val interval =
                    runCatching { SettingsStore(context).intervalSeconds.first() }
                        .getOrDefault(SettingsStore.DEFAULT_INTERVAL)
                ids.forEach { id -> renderWidget(context, manager, id, interval) }
                WidgetRefreshWorker.schedule(context)
            } finally {
                pendingResult.finish()
            }
        }
    }''',
          )
          replace(
              provider,
              '''        manager: AppWidgetManager,
        appWidgetId: Int,
    ) {''',
              '''        manager: AppWidgetManager,
        appWidgetId: Int,
        intervalSeconds: Int,
    ) {''',
          )
          replace(
              provider,
              '''                setRemoteAdapter(R.id.news_flipper, serviceIntent)
                setEmptyView(R.id.news_flipper, R.id.widget_empty)''',
              '''                setRemoteAdapter(R.id.news_flipper, serviceIntent)
                setEmptyView(R.id.news_flipper, R.id.widget_empty)
                setInt(R.id.news_flipper, "setFlipInterval", intervalSeconds.coerceIn(3, 120) * 1_000)''',
          )
          replace(
              provider,
              '''                setOnClickPendingIntent(R.id.widget_refresh, refreshPendingIntent(context, appWidgetId))''',
              '''                setOnClickPendingIntent(R.id.widget_refresh, actionPendingIntent(context, appWidgetId, ACTION_REFRESH, "refresh"))
                setOnClickPendingIntent(R.id.widget_previous, actionPendingIntent(context, appWidgetId, ACTION_SHOW_PREVIOUS, "previous"))
                setOnClickPendingIntent(R.id.widget_next, actionPendingIntent(context, appWidgetId, ACTION_SHOW_NEXT, "next"))''',
          )
          replace(
              provider,
              '''    private fun refreshPendingIntent(
        context: Context,
        appWidgetId: Int,
    ): PendingIntent {
        val intent =
            Intent(context, WidgetActionReceiver::class.java).apply {
                action = ACTION_REFRESH
                putExtra(AppWidgetManager.EXTRA_APPWIDGET_ID, appWidgetId)
                // Unique per widget so the PendingIntents don't collapse into one.
                data = Uri.parse("kanarek://refresh/$appWidgetId")
            }
        return PendingIntent.getBroadcast(
            context,
            appWidgetId,
            intent,
            PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE,
        )
    }''',
              '''    private fun actionPendingIntent(
        context: Context,
        appWidgetId: Int,
        actionName: String,
        path: String,
    ): PendingIntent {
        val intent =
            Intent(context, WidgetActionReceiver::class.java).apply {
                action = actionName
                putExtra(AppWidgetManager.EXTRA_APPWIDGET_ID, appWidgetId)
                // Unique per action and widget so PendingIntents cannot collapse into one.
                data = Uri.parse("kanarek://$path/$appWidgetId")
            }
        return PendingIntent.getBroadcast(
            context,
            (appWidgetId * 10) + path.hashCode(),
            intent,
            PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE,
        )
    }''',
          )
          replace(
              provider,
              '''        const val ACTION_REFRESH = "com.kanarek.action.REFRESH"

        /** Triggers a data refresh on every kanarek widget on screen. */''',
              '''        const val ACTION_REFRESH = "com.kanarek.action.REFRESH"
        const val ACTION_SHOW_PREVIOUS = "com.kanarek.action.SHOW_PREVIOUS"
        const val ACTION_SHOW_NEXT = "com.kanarek.action.SHOW_NEXT"

        /** Re-renders every news widget, including slideshow controls and interval. */
        fun updateAll(context: Context) {
            val manager = AppWidgetManager.getInstance(context)
            val ids = manager.getAppWidgetIds(ComponentName(context, KanarekWidgetProvider::class.java))
            if (ids.isEmpty()) return
            context.sendBroadcast(
                Intent(context, KanarekWidgetProvider::class.java).apply {
                    action = AppWidgetManager.ACTION_APPWIDGET_UPDATE
                    putExtra(AppWidgetManager.EXTRA_APPWIDGET_IDS, ids)
                },
            )
        }

        /** Triggers a data refresh on every kanarek widget on screen. */''',
          )

          receiver = "kanarek/app/src/main/java/com/kanarek/widget/WidgetActionReceiver.kt"
          replace(
              receiver,
              '''import android.net.Uri
import android.widget.RemoteViews''',
              '''import android.net.Uri
import android.widget.RemoteViews''',
          ) if False else None
          replace(
              receiver,
              '''import android.content.Intent
import androidx.core.content.ContextCompat''',
              '''import android.content.Intent
import android.widget.RemoteViews
import androidx.core.content.ContextCompat''',
          )
          replace(
              receiver,
              '''            KanarekWidgetProvider.ACTION_REFRESH -> refreshNewsWidget(context, intent)
            PlayerWidgetProvider.ACTION_TOGGLE -> startPlayer(context, PlayerService.ACTION_TOGGLE)''',
              '''            KanarekWidgetProvider.ACTION_REFRESH -> refreshNewsWidget(context, intent)
            KanarekWidgetProvider.ACTION_SHOW_PREVIOUS -> navigateNewsWidget(context, intent, forward = false)
            KanarekWidgetProvider.ACTION_SHOW_NEXT -> navigateNewsWidget(context, intent, forward = true)
            PlayerWidgetProvider.ACTION_TOGGLE -> startPlayer(context, PlayerService.ACTION_TOGGLE)''',
          )
          replace(
              receiver,
              '''    private fun startPlayer(
        context: Context,
        action: String,
    ) {''',
              '''    private fun navigateNewsWidget(
        context: Context,
        intent: Intent,
        forward: Boolean,
    ) {
        val manager = AppWidgetManager.getInstance(context)
        val id = intent.getIntExtra(AppWidgetManager.EXTRA_APPWIDGET_ID, AppWidgetManager.INVALID_APPWIDGET_ID)
        if (id == AppWidgetManager.INVALID_APPWIDGET_ID) return
        val views = RemoteViews(context.packageName, R.layout.widget)
        if (forward) views.showNext(R.id.news_flipper) else views.showPrevious(R.id.news_flipper)
        manager.partiallyUpdateAppWidget(id, views)
    }

    private fun startPlayer(
        context: Context,
        action: String,
    ) {''',
          )

          reader = "kanarek/app/src/main/java/com/kanarek/ui/ReaderScreen.kt"
          replace(
              reader,
              '''    val headlinesMode by settings.headlinesMode.collectAsStateWithLifecycle(initialValue = false)
    val topSources by settings.topSources.collectAsStateWithLifecycle(initialValue = emptySet())
    val perSourceCap by settings.perSourceCap.collectAsStateWithLifecycle(initialValue = 0)''',
              '''    val headlinesMode by settings.headlinesMode.collectAsStateWithLifecycle(initialValue = false)
    val topSources by settings.topSources.collectAsStateWithLifecycle(initialValue = emptySet())
    val perSourceCap by settings.perSourceCap.collectAsStateWithLifecycle(initialValue = 0)
    val intervalSeconds by settings.intervalSeconds.collectAsStateWithLifecycle(initialValue = SettingsStore.DEFAULT_INTERVAL)''',
          )
          replace(
              reader,
              '''                    Text(
                        stringResource(R.string.widget_hint),
                        style = MaterialTheme.typography.bodySmall,
                    )

                    Spacer(Modifier.height(4.dp))''',
              '''                    Text(
                        stringResource(R.string.widget_hint),
                        style = MaterialTheme.typography.bodySmall,
                    )

                    Text(stringResource(R.string.widget_interval), style = MaterialTheme.typography.labelLarge)
                    Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {
                        listOf(5, 7, 10, 15, 30).forEach { seconds ->
                            FilterChip(
                                selected = intervalSeconds == seconds,
                                onClick = {
                                    scope.launch {
                                        settings.setIntervalSeconds(seconds)
                                        KanarekWidgetProvider.updateAll(context)
                                    }
                                },
                                label = { Text(stringResource(R.string.widget_interval_seconds, seconds)) },
                            )
                        }
                    }

                    Spacer(Modifier.height(4.dp))''',
          )

          en = "kanarek/app/src/main/res/values/strings.xml"
          replace(en, '''    <string name="widget_refresh">Refresh</string>''', '''    <string name="widget_refresh">Refresh</string>
    <string name="widget_previous_story">Previous story</string>
    <string name="widget_next_story">Next story</string>''')
          replace(en, '''    <string name="delete_station">Delete station</string>''', '''    <string name="delete_station">Delete station</string>
    <string name="add_station_favorite">Add to favorites</string>
    <string name="remove_station_favorite">Remove from favorites</string>''')
          replace(en, '''    <string name="filter_other">Other</string>''', '''    <string name="filter_other">Other</string>
    <string name="filter_favorites">Favorites</string>''')
          replace(en, '''    <string name="widget_hint">Add the Kanarek widget from your launcher\'s widget picker, then drag a corner to resize it.</string>''', '''    <string name="widget_hint">Add the Kanarek widget from your launcher\'s widget picker, then drag a corner to resize it.</string>
    <string name="widget_interval">Widget slideshow speed</string>
    <string name="widget_interval_seconds">%1$d s</string>''')

          pl = "kanarek/app/src/main/res/values-pl/strings.xml"
          replace(pl, '''    <string name="widget_refresh">Odśwież</string>''', '''    <string name="widget_refresh">Odśwież</string>
    <string name="widget_previous_story">Poprzedni artykuł</string>
    <string name="widget_next_story">Następny artykuł</string>''')
          replace(pl, '''    <string name="delete_station">Usuń stację</string>''', '''    <string name="delete_station">Usuń stację</string>
    <string name="add_station_favorite">Dodaj do ulubionych</string>
    <string name="remove_station_favorite">Usuń z ulubionych</string>''')
          replace(pl, '''    <string name="filter_other">Inne</string>''', '''    <string name="filter_other">Inne</string>
    <string name="filter_favorites">Ulubione</string>''')
          replace(pl, '''    <string name="widget_hint">Dodaj widget Kanarka z listy widgetów launchera, potem złap za róg, żeby zmienić rozmiar.</string>''', '''    <string name="widget_hint">Dodaj widget Kanarka z listy widgetów launchera, potem złap za róg, żeby zmienić rozmiar.</string>
    <string name="widget_interval">Tempo slajdów widgetu</string>
    <string name="widget_interval_seconds">%1$d s</string>''')

          Path("kanarek/app/src/main/res/layout/widget.xml").write_text('''<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <AdapterViewFlipper
        android:id="@+id/news_flipper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:autoStart="true"
        android:flipInterval="7000"
        android:inAnimation="@android:anim/fade_in"
        android:outAnimation="@android:anim/fade_out" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="12dp"
        android:text="@string/widget_loading"
        android:textColor="#B0B8C4"
        android:textSize="13sp" />

    <ImageButton
        android:id="@+id/widget_refresh"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="top|end"
        android:layout_margin="6dp"
        android:background="@drawable/refresh_bg"
        android:contentDescription="@string/widget_refresh"
        android:padding="7dp"
        android:src="@drawable/ic_refresh" />

    <ImageButton
        android:id="@+id/widget_previous"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="bottom|start"
        android:layout_margin="6dp"
        android:background="@drawable/refresh_bg"
        android:contentDescription="@string/widget_previous_story"
        android:padding="7dp"
        android:src="@drawable/ic_widget_previous" />

    <ImageButton
        android:id="@+id/widget_next"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="6dp"
        android:background="@drawable/refresh_bg"
        android:contentDescription="@string/widget_next_story"
        android:padding="7dp"
        android:src="@drawable/ic_widget_next" />
</FrameLayout>
''')
          Path("kanarek/app/src/main/res/drawable/ic_widget_previous.xml").write_text('''<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M6,6h2v12H6zM18,6v12l-8.5,-6z" />
</vector>
''')
          Path("kanarek/app/src/main/res/drawable/ic_widget_next.xml").write_text('''<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M16,6h2v12h-2zM6,6v12l8.5,-6z" />
</vector>
''')

          readme = "kanarek/README.md"
          replace(
              readme,
              '''- **Dynamic slideshow** — an `AdapterViewFlipper` auto-advances through fetched headlines
  (launcher auto-advance + self-starting flipper, with fade transitions). A refresh button
  re-pulls on demand.''',
              '''- **Dynamic slideshow** — an `AdapterViewFlipper` auto-advances through fetched headlines
  (launcher auto-advance + self-starting flipper, with fade transitions). Previous/next controls
  allow manual browsing, refresh re-pulls on demand, and the companion app exposes a 5–30 second
  slideshow interval that is applied to every news widget.''',
          )
          replace(
              readme,
              '''- **TV vs radio, visibly** — every station row (and the now-playing bar) carries a small''',
              '''- **Favorite stations** — star any radio or TV station from its row or the now-playing bar.
  Favorites live in DataStore (separate from portable M3U data), survive re-imports through stable
  station ids, and appear in their own tab; changing a favorited stream URL migrates the star.
- **TV vs radio, visibly** — every station row (and the now-playing bar) carries a small''',
          )

          history = "kanarek/docs/HISTORY.md"
          replace(
              history,
              '''## Zrobione (chronologicznie)
''',
              '''## Zrobione (chronologicznie)
- **Ulubione stacje + sterowanie widgetem newsów**: radio/TV można oznaczać gwiazdką z wiersza
  lub belki „teraz gra”, a osobna zakładka Ulubione filtruje stabilne identyfikatory stacji zapisane
  w DataStore (bez zaśmiecania eksportu M3U). Usunięcie stacji czy zmiana jej URL sprząta/migruje
  wpis. Widget wiadomości dostał ręczne poprzedni/następny i działające ustawienie interwału
  5–30 s; dotychczasowy `intervalSeconds` był martwym ustawieniem i nie sterował flipperem.
''',
          )

          for path in [
              "kanarek/app/src/main/res/layout/widget.xml",
              "kanarek/app/src/main/res/drawable/ic_widget_previous.xml",
              "kanarek/app/src/main/res/drawable/ic_widget_next.xml",
              en,
              pl,
          ]:
              ET.parse(path)
          PY

          rm .github/workflows/kanarek-favorites-widget-bootstrap.yml
          git diff --check

      - name: Commit feature
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(kanarek): add favorites and widget controls"
          git push origin HEAD:feat/kanarek-favorites-widget-controls
